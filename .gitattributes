# Normalise to LF in the repository. The GitHub Actions runner is Ubuntu and the
# workflow embeds shell heredocs, which CRLF would break.
* text=auto eol=lf

*.png binary
*.jpg binary
*.zip binary
*.parquet binary
